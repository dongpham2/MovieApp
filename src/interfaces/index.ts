export interface dataMoviesType {
    banner: string;
    title: string;
    publish: number;
    review: number;
}

export interface dataMoviesSearch {
    title: string;
    year: number;
    genres: object;
    thumbnail: string;
}

export interface slideInterface {
  img : string;
}